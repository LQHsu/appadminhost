// Versión de producción de api.config.ts — Angular la intercambia por
// la de desarrollo automáticamente en `ng build` (ver "fileReplacements"
// en angular.json, configuración "production"). `ng serve` local sigue
// usando api.config.ts (localhost:3000) sin tocar esto.
export const API_URL = 'https://hostal-backend-kd64.onrender.com';

export const REQUIERE_CLAVE = true;
